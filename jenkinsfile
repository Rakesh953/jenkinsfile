pipeline {
    agent any

    stages {
        stage('code and build') {
            steps {
               git 'https://github.com/Rakesh953/jenkinsfile.git'
               sh 'mvn clean package'
            }
        }
    }
}
